# GIMP Plugin

Target area for the GIMP-side plugin and execution bridge.

Current implementation:

- `gimp-mcp-plugin.py`

The plugin is intentionally not moved yet because the local install and execution flow still depend on the current file layout.
